export class Graph {
    constructor(data, options) {
        this.data = data;
        this.options = options;
    }

    render(node) {
    }
}

export function hello() {
    return 'hello world';
}
